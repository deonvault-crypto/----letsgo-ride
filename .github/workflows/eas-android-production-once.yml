name: EAS Android Production Build 2

on:
  push:
    branches:
      - automation/android-production-build-14b124b4

permissions:
  contents: read

concurrency:
  group: eas-android-production-8c6c7112
  cancel-in-progress: false

jobs:
  build-android-production:
    name: Build Android production 8c6c7112
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: mobile
    steps:
      - name: Checkout corrected release candidate
        uses: actions/checkout@v4
        with:
          ref: 8c6c71123e126111757268c009e45cc45aba5328

      - name: Verify exact source SHA
        working-directory: .
        shell: bash
        run: |
          set -euo pipefail
          actual="$(git rev-parse HEAD)"
          expected="8c6c71123e126111757268c009e45cc45aba5328"
          echo "Source SHA: $actual"
          test "$actual" = "$expected"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Install dependencies reproducibly
        run: npm ci

      - name: Verify Android release policy
        run: npm run verify:android

      - name: Verify Expo authentication
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN repository secret is required."
            exit 1
          fi
          npx eas-cli@latest whoami --non-interactive

      - name: Build production Android AAB on EAS
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx eas-cli@latest build --platform android --profile production --non-interactive
