{
  "name": "letsgoride-ops-web",
  "version": "1.0.0",
  "private": true,
  "description": "LetsGoRide Operations Control Center",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check check-liveness.js && node check-public-scripts.js && node check-liveness.js"
  },
  "engines": {
    "node": ">=20"
  }
}
