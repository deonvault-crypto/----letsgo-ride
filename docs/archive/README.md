# Archived LetsGoRide documentation

Files under `docs/archive/` are retained for historical context only. They may describe superseded branches, release procedures, architecture decisions, launch plans or operational assumptions.

They are **not** the current source of truth.

For current production behavior and release rules, use:

- the repository root `README.md`
- the code on `platform-v2-m1-m6`
- permanent workflows under `.github/workflows/`
- current production configuration and tests

Do not restore an archived instruction or configuration merely because it appears in repository search. Validate it against current source before using it.
